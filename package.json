{
  "name": "quiz-platform",
  "version": "1.0.0",
  "description": "QuizMaster - A cloud-based assessment platform for educators and students",
  "scripts": {
    "install:server": "cd server && npm install",
    "install:client": "cd client && npm install",
    "install:all": "npm run install:server && npm run install:client",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "build:client": "cd client && npm run build",
    "start:server": "cd server && npm start",
    "migrate": "cd server && npx knex migrate:latest",
    "seed": "cd server && npx knex seed:run",
    "start": "cd server && npx knex migrate:latest --env production && node src/app.js"
  }
}
